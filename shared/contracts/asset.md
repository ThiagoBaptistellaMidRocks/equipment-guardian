# Asset Contract

The canonical machine-readable contract is `asset.schema.json`.

The current supported asset types are:

- `HAUL_TRUCK`
- `EXCAVATOR`
- `DRILL`

This contract describes asset identity, asset health, and asset telemetry only. It does not contain prediction logic.
