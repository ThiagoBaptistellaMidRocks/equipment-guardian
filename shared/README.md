# Shared

Shared contracts, examples, and interface definitions for Equipment Guardian.

Use this folder as the source of truth for cross-boundary types. Frontend, backend, data, and ML code should align with these contracts instead of duplicating assumptions.
